class {{TEMPLATE_NAME}} {
  final int id;
  final String name;

  const {{TEMPLATE_NAME}}({
    required this.id,
    required this.name,
  });

  @override
  bool operator ==(Object other) {
    if (identical(this, other)) return true;
    return other is {{TEMPLATE_NAME}} &&
        other.id == id &&
        other.name == name;
  }

  @override
  int get hashCode => id.hashCode ^ name.hashCode;

  @override
  String toString() => '{{TEMPLATE_NAME}}(id: $id, name: $name)';
}